# Public client configuration (safe to expose in the browser bundle)
VITE_SUPABASE_URL=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_PROJECT_ID=""

# Server-side only. Never commit real values; store them as project secrets.
SUPABASE_URL=""
SUPABASE_PUBLISHABLE_KEY=""
# SUPABASE_SERVICE_ROLE_KEY  - managed secret, used only by server code / the Python AI service
# STREAM_TICKET_SECRET       - managed secret, signs short-lived live-stream tickets
